# fanqie.koplugin

KOReader 插件，用于在电纸书设备上阅读番茄小说。

## 功能特性

- 📚 **书架管理**：浏览和搜索番茄小说，支持封面显示
- 📖 **章节阅读**：支持章节导航、自动翻页、目录自动跳转
- 🔄 **进度同步**：自动同步阅读进度到服务器
- ⚡ **预下载**：智能预下载后续章节，减少阅读等待（含限流保护，不阻塞阅读）
- 💬 **段评功能**：正文中显示段评数字气泡，点击查看段评详情
- 🔀 **多书源**：支持大灰狼、晴天、官方API 三个书源，自动检测可用线路
- 🎨 **界面优化**：适配墨水屏显示，支持黑白模式
- 📊 **阅读统计**：显示阅读进度（百分比/章节数）
- 📥 **批量下载**：支持下载全书，自动跳过已缓存章节，含限流等待
- 📋 **下载管理**：查看当前下载进度、取消任务、浏览历史记录
- 📶 **离线阅读**：书架和章节缓存持久化，无网络也可阅读已缓存内容
- 🔀 **书架排序**：支持按阅读进度、最后阅读时间、加入时间、书名排序

## 安装方法

1. 下载插件压缩包（从 GitHub Releases 页面）
2. 将 `fanqie.koplugin` 文件夹复制到 KOReader 的 `plugins/` 目录
3. 将 `config.example.lua` 复制一份，改名为 `config.lua`
4. 在 `config.lua` 中配置书源账号信息（获取方法见下方）
5. 重启 KOReader
6. 在菜单中找到「番茄书架」入口

## 书源说明

本插件支持三个书源，按优先级从高到低排列：

| 书源 | 说明 | 排序 |
|------|------|------|
| **大灰狼** | 聚合平台，支持番茄/七猫/塔读/QQ阅读等底层源，支持段评 | 1（默认首选） |
| **晴天** | 聚合平台，支持番茄底层源 | 2（备用） |
| **官方API** | 番茄官方接口，需 Cookie | 3（备用） |

插件会按排序依次尝试可用书源。大灰狼排第一位，获取正文失败时自动回退到晴天，再回退到官方API。

## 大灰狼书源配置

### 第一步：注册账号

1. 浏览器访问 `https://v2.czyl.cf`（大灰狼官网）
2. 注册账号（邮箱 + 密码）
3. 注册成功后，将账号密码填入 `config.lua`

### 第二步：填写配置

编辑 `config.lua`，找到 `dahuilang` 配置段：

```lua
dahuilang = {
    server_url = "https://v2.czyl.cf",
    servers = {
        "https://v2.czyl.cf",
        "https://v4.czyl.cf",
        "https://v5.czyl.cf",
    },
    username = "你的邮箱@example.com",  -- ← 填入注册邮箱
    password = "你的密码",              -- ← 填入密码
    auto_login = true,                  -- token过期自动重新登录
    source = "番茄",                    -- 底层书源
},
```

### 第三步：在 KOReader 中登录

1. 打开 KOReader → 菜单 → 番茄小说 → 设置 → 书源管理
2. 点击「大灰狼」
3. 点击「检测可用线路」— 自动检测最快可用服务器
4. 点击「未登录 (点击立即登录)」— 自动登录并获取 token
5. 登录成功后即可使用

> **提示**：`auto_login = true` 时，token 过期会自动重新登录，无需手动干预。

## 番茄 Cookie 获取方式（官方API书源）

### 方法一：浏览器开发者工具（推荐）

1. 打开浏览器（推荐 Chrome 或 Edge）
2. 访问 [番茄小说网页版](https://fanqie.com)
3. 登录你的账号
4. 按下 `F12` 打开开发者工具
5. 切换到「Network」（网络）标签页
6. 在页面上点击「书架」或刷新页面，触发网络请求
7. 在网络请求列表中找到 `multidetail` 请求（URL 类似 `https://fanqienovel.com/api/bookshelf/multidetail?...`），点击查看详情
8. 在「Headers」（请求头）中找到 `Cookie` 字段
9. 右键点击 `Cookie` 值，选择「Copy value」（复制值）
10. 将复制的完整 Cookie 字符串粘贴到 `config.lua` 的 `cookie_string` 中

### 方法二：浏览器扩展

安装 Cookie 导出插件（如「EditThisCookie」），一键导出 Cookie 字符串。

## 配置说明

将 `config.example.lua` 复制一份，改名为 `config.lua`，然后根据需要修改配置项：

```lua
return {
    -- 番茄官方 Cookie（用于书架和目录，官方API书源需要）
    cookie_string = "serial_uuid=xxx; sessionid=xxx; ttwid=xxx; ...",

    -- 大灰狼书源配置（推荐首选）
    dahuilang = {
        server_url = "https://v2.czyl.cf",
        servers = {
            "https://v2.czyl.cf",
            "https://v4.czyl.cf",
            "https://v5.czyl.cf",
        },
        username = "",          -- 注册邮箱
        password = "",          -- 密码
        key = "",               -- 密钥（可选，优先于账号密码）
        auto_login = true,      -- token过期自动登录
        source = "番茄",        -- 底层书源
    },

    -- 晴天书源配置（备用）
    qingtian = {
        server_url = "https://v1.gyks.cf/",
        servers = {
            "https://v1.gyks.cf",
            "https://v2.gyks.cf",
            "https://v3.gyks.cf",
        },
        username = "",
        password = "",
        auto_login = true,
    },

    -- 进度同步
    sync = {
        pull_on_open = true,       -- 打开书籍时拉取云端进度
        upload_on_close = true,    -- 关闭书籍时上传进度
    },

    -- 缓存设置
    cache = {
        download_book_images = true,
        pre_download_chapters = 3,  -- 预下载章节数
    },
}
```

## 使用指南

### 首次使用

1. **安装插件**：将 `fanqie.koplugin` 放入 KOReader 的 `plugins/` 目录
2. **配置书源**：编辑 `config.lua`，至少配置一个书源（推荐大灰狼）
3. **重启 KOReader**
4. **进入书架**：菜单 → 番茄小说 → 书架
5. **登录书源**：菜单 → 番茄小说 → 设置 → 书源管理 → 大灰狼 → 登录

### 书源管理

路径：菜单 → 番茄小说 → 设置 → 书源管理

- **启用/禁用**：点击书源名称，选择「禁用此源」或「启用此源」
- **检测线路**：点击「检测可用线路」，自动选择最快服务器
- **登录/退出**：点击「未登录 (点击立即登录)」登录，或点击「已登录 (点击退出)」退出

### 书架操作

- **点击书籍**：打开书籍详情页面
- **点击「刷新书架」**：重新获取书架数据
- **长按书籍**：显示操作菜单（开始阅读、章节目录、清空缓存）

### 阅读操作

- **点击左侧**：上一页
- **点击右侧**：下一页
- **点击中间**：打开菜单
- **章节开头向前翻页**：自动跳转到上一章
- **章节结尾向后翻页**：自动跳转到下一章

### 段评功能

1. **开启段评**：菜单 → 番茄小说 → 段评 → 段评: 开 (点击开启)
2. **查看段评**：正文中看到段评数字（如 `47`），点击即可查看段评详情
3. **段评详情**：使用可滚动界面展示全部评论，支持上一段/下一段切换
4. **关闭段评**：菜单 → 番茄小说 → 段评 → 段评: 关 (点击关闭)

> **注意**：段评功能仅大灰狼书源支持。开启后会重新获取章节内容（带段评标记）。

### 目录操作

- **打开目录**：自动跳转到当前阅读章节所在页面
- **点击章节**：跳转到对应章节
- **缓存标记**：已下载的章节显示 ✓ 标记

### 下载管理

路径：菜单 → 番茄小说 → 下载管理

- **查看进度**：当前下载任务显示实时进度条和章节信息
- **取消下载**：点击「取消下载」可中止当前任务
- **后台运行**：下载进度对话框可关闭，下载在后台继续
- **历史记录**：显示最近 10 条下载记录（完成/中断/取消）
- **清空历史**：点击「清空历史记录」清除所有历史

> **说明**：下载状态持久化到本地，即使 KOReader 退出重启，历史记录依然保留。未完成的下载会标记为「中断」。

### 离线阅读

- **书架离线**：书架数据自动缓存，无网络时显示缓存内容
- **章节离线**：已下载的章节无网络也可阅读
- **自动定位**：离线打开书籍时，从上次阅读的章节开始（书架数据中的进度）
- **缓存优先**：有网络时仍会后台刷新，保证数据最新

## ⚠️ 重要声明

### 接口说明

本插件使用的正文获取接口为第三方公开接口，**非官方 API**。

### 使用规范

1. **请勿一次性下载全本书**：插件默认仅预下载 3 章，请保持此设置
2. **请勿滥用接口**：频繁请求可能导致 IP 被封禁
3. **请合理阅读**：建议每天阅读时长不超过 2 小时，保护视力

### 免责声明

本插件仅供**技术分享和学习研究**目的使用：

- 本插件不提供任何小说内容，所有内容均来自公开网络
- 用户使用本插件产生的一切后果由用户自行承担
- 请尊重版权，支持正版阅读
- 如涉及侵权，请联系作者删除

## 常见问题

### Q: 书架显示为空？

A: 请检查 `config.lua` 中的 Cookie 是否正确，Cookie 过期会导致无法获取书架数据。书架和目录功能依赖番茄官方 Cookie。

### Q: 章节内容获取失败？

A: 可能是书源服务器不可用。请进入「书源管理」→「检测可用线路」切换服务器，或尝试登录其他书源。

### Q: 段评不显示？

A: 段评功能仅大灰狼书源支持。请确保：1) 大灰狼书源已启用并登录；2) 段评开关已开启；3) 点击「刷新段评数据」重新获取章节。

### Q: 点击段评数字没反应？

A: 请确保已更新到最新版本，并重启 KOReader 使补丁生效。

### Q: 阅读进度不同步？

A: 请确保网络连接正常，进度同步需要网络支持。

### Q: 封面不显示？

A: 封面是异步下载的，首次打开书架时需要等待一段时间。如果长时间不显示，请检查网络连接。

## 更新日志

### v2.1.0
- ✅ 下载管理界面：实时进度条、取消任务、历史记录、清空历史
- ✅ 下载状态持久化：KOReader 退出重启后保留历史记录，未完成标记为「中断」
- ✅ 离线阅读：书架缓存 + 章节缓存，无网络也可阅读
- ✅ 离线自动定位：从书架数据中的上次阅读章节开始
- ✅ 预下载限流保护：滑动时间窗口算法，防止服务器封禁
- ✅ 预下载独立标志：`pre_downloading` 不阻塞阅读，用户操作优先中断预下载
- ✅ 下载限流时间戳跨子进程合并
- ✅ 后台运行修复：关闭进度对话框后下载继续，不闪退
- ✅ 书架缓存转义修复：书名含特殊字符不再导致缓存加载失败
- ✅ Menu 参数修复：`item_table` 替代 `items`，确保菜单项正确显示
- ✅ 异步化更多操作：目录获取、书籍打开、书架获取、段评详情、服务器检测、登录

### v2.0.0
- ✅ 段评功能：正文中显示段评数字气泡，点击查看段评详情
- ✅ 段评展示使用 TextViewer 组件，支持滚动浏览和段落切换
- ✅ 多书源支持：大灰狼（首选）、晴天（备用）、官方API（备用）
- ✅ 服务器自动检测：自动选择最快可用线路
- ✅ Token 自动刷新：token 过期自动重新登录
- ✅ 书源管理界面：启用/禁用、检测线路、登录/退出
- ✅ 服务器检测速度优化（首次加载从 22s 降至 6-8s）
- ✅ 段评状态持久化：重启后保持段评开关状态
- ✅ 章节正文广告清理（末尾广告、不可见字符）
- ✅ 图片段落样式优化（去缩进、居中、限宽）

### v1.2.2
- ✅ 书架封面异步下载
- ✅ 目录自动跳转到当前阅读章节
- ✅ 下载全书跳过已缓存章节
- ✅ 书架排序功能（阅读进度、最后阅读时间、加入时间、书名）
- ✅ 修复章节索引 bug
- ✅ 目录界面简化（去掉刷新/跳转按钮）

### v1.2.0
- ✅ 下载进度对话框
- ✅ 书架封面支持
- ✅ 下载延迟和错误处理

### v1.1.3
- ✅ 修复批量下载闪退问题
- ✅ 添加章节下载间隔
- ✅ 添加失败计数

### v1.1.0
- ✅ 简化 Cookie 配置（支持直接粘贴 Cookie 字符串）
- ✅ 修复书架缓存问题
- ✅ 修复章节向前翻页问题

### v1.0.0
- ✅ 初始版本
- ✅ 书架管理
- ✅ 章节阅读
- ✅ 进度同步
- ✅ 预下载功能

## 技术说明

- 基于 Lua 语言开发，适配 KOReader 环境
- 使用 REST API 获取数据
- 本地缓存机制，减少网络请求
- 支持墨水屏黑白显示优化
- 段评通过 `fanqie-para:N` 自定义 URL 方案 + ReaderLink 补丁实现点击交互

## 许可证

MIT License
