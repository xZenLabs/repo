# v3.9.0

### 新功能

- **标注 / 笔记统计**：书单列表新增「标注」「笔记」两列，分别统计每本书的高亮总数与带笔记的高亮数。数据从各书 DocSettings 的 `highlight` 设置读取，按书名缓存避免重复计算。

- **翻译词条**：新增 `col_anno`（标注 / Marks / 標註）和 `col_note`（笔记 / Notes / 筆記），覆盖简体中文、繁体中文（香港）、英文三语。

### 改进

- **字体对象缓存**：新增 `_font_cache` 表，按 `字体名@字号` 缓存 Font 对象，避免重复加载内置 24MB 汇文明朝体，显著提升渲染速度。

- **drawBoxText 增强**：新增 `fg`（前景色）和 `bg`（背景色）参数，默认黑底白字，支持自定义颜色。

- **轻量模式字号动态缩放**：引入轻量系数 `lmf = 0.72`，在低内存模式下同步下调各档字号下限，避免低分辨率下文字相对过大导致元素重叠。

- **列布局动态计算**：列起点与宽度改为基于实际字号动态计算（含 `col_gap`、`num_col_w` 等），替代原来的固定像素间距。

### 变更

- **书单列表列头**：由「数量 / 单位」（显示 `1` / `本`）改为「标注 / 笔记」（显示实际数字）。

- **版本号**：`_meta.lua` 版本由 `3.9.0` → `3.9.0beta1`；`main.lua` 内部 `PLUGIN_VERSION` 由 `3.9.0beta1` → `3.9.1`。

### 移除 / 禁用

- **低内存模式菜单入口**：菜单项被注释关闭（设置项 `low_memory_mode` 及底层逻辑保留，如需恢复取消注释即可）。

# v3.8.2

### 3.8.2 更新摘要

**新增**

墨痕阅读统计面板（全屏，阅读时长前 9 本，含品类/作者/进度/本期时长）
手势调出 + 右上角关闭键
设置项「品牌署名（墨痕）」自定义字样
设置菜单「加入内测」入口（QQ群:627525507 提示框）
resources/ 目录：透明 Logo + 导航栏方形 Logo

**修复**
透明 Logo 黑块（改用 ImageWidget alpha 绘制）
ZenOS 导航栏「添加操作」闪退（general=true + callback）
「加入内测」提示框闪退（改用原生 InfoMessage）

**改进**
导航栏换墨痕 Logo、统计文案中文化、加入内测提示框停留 10 秒

**其他**
main.lua 重构统计渲染；新增 stats_screen.lua；版本号升 3.8.2

# v3.8.1

**新增**：热力图改最近 26 周（对齐周一）、灰阶配色（5 级灰度，参考 Simple UI） 修复：标题不显示（原版 po 无 heatmap_title，返回空串；现加硬编码兜底）、版本号不一致（main.lua 的 PLUGIN_VERSION 是 误导 OTA 检查，已统一） 
**变更**：宽度与二维码对齐、去掉图例、月份标签上移、高度 11.5%→14%（仅热力图）

# v3.7.2

修复打包问题

# v3.7.1

### v3.7.1

移植 bookends 字体选择器：可视化选择壁纸字体，分页浏览 + 实时预览
​
修复变量名冲突导致菜单崩溃： local _, update  覆盖 gettext 函数  _ ，触发  attempt to call local '_' (a table value) 
​
改进书单补充逻辑：微信读书书单不足 top_n 时从觅阅进度表补充

修复"本周期暂无显示书单"：微信读书缓存不可用时缺少从觅阅进度表补充书单的兜底逻辑
​
新增统一兜底：所有数据源分支后，书单为空且进度表有数据就按进度排序补充
​
精简重复的补充代码块
