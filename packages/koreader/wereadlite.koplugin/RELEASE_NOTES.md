# v0.2.5

## [0.2.5]

### Fixed

- 优化超大章节的加载体验
- 优化评论弹窗的体验
- 网络稳定性修复

---

### 安装

将 `wereadlite.koplugin-v0.2.5.zip` 解压到 KOReader 的 `plugins/` 目录。

请勿打包进 `data/`、`cache/`（会话与缓存数据）。

# v0.2.4

## [0.2.4]

### Fixed

- 优化评论展示
- KOBO版修复
- 书籍渲染问题修复

---

### 安装

将 `wereadlite.koplugin-v0.2.4.zip` 解压到 KOReader 的 `plugins/` 目录。

请勿打包进 `data/`、`cache/`（会话与缓存数据）。

# v0.2.3

## [0.2.3]

### Fixed

- 优化评论展示
- 其他稳定性修复

---

### 安装

将 `wereadlite.koplugin-v0.2.3.zip` 解压到 KOReader 的 `plugins/` 目录。

请勿打包进 `data/`、`cache/`（会话与缓存数据）。

# v0.2.2

## [0.2.2]

### Added

- 图书详情页、相似推荐（书架长按）
- 「为你推荐」组件卡片

### Fixed

- 优化整体性能
- 优化热门划线与评论加载

---

### 安装

将 `wereadlite.koplugin-v0.2.2.zip` 解压到 KOReader 的 `plugins/` 目录。

请勿打包进 `data/`、`cache/`（会话与缓存数据）。

# v0.2.1

## wereadlite v0.2.1

安装：将 `wereadlite.koplugin-v0.2.1.zip` 解压到 KOReader 的 `plugins/` 目录。

请勿打包进 `data/`、`cache/`（会话与缓存数据）。


**Full Changelog**: https://github.com/Exhen/wereadlite.koplugin/compare/v0.2.0...v0.2.1
