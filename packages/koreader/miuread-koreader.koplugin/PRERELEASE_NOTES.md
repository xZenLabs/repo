- 新下载正式切换为“唯一正文 + 独立评论缓存”模型：主页、书架二级菜单、整本下载、单章下载和章节范围下载不再提供“纯净版 / 划线与想法版”二选一；评论不会再决定生成第二份 EPUB。
- 整本正文下载新增评论预加载选择：支持不预加载、前 1 / 3 / 5 / 10 章或整本，默认前 5 章；正文生成完成即视为下载成功，评论预加载独立执行，失败或暂停不会影响书籍阅读。
- 单章节下载默认同时获取该章评论；章节范围下载默认只取正文，可按需切换为同时获取所选章节评论。评论数据继续统一写入 `thoughts.sqlite3`，不修改正文文件。
- 明确区分“下一章正文预读取”和“下一章评论预加载”：两者各自独立调度；自动准备下一章正文统一生成 clean 正文，不再沿用旧 notes 版本继续制造新的评论 EPUB。
- 微信书架继续保持“点击书籍进入二级菜单”的现有交互；二级菜单收口为阅读、正文下载/更新、部分章节、评论、书籍详情与本地文件管理，不新增长按操作。
- 首页最近阅读与书架状态取消“纯净版 / 含评论 / 两个版本”等版本标签；若设备同时保留旧 clean/notes 文件，新逻辑优先统一正文，同时继续允许打开已有旧 notes 文件。
- 当前书籍的重新生成、章节扩展与重新下载入口统一为“正文”；评论获取、收藏和缓存独立管理，重新生成正文不会删除评论缓存或评论收藏。
- 下载管理将新任务显示为正文、章节正文或单章正文；旧 `annotations=true` 队列仅标记为兼容任务。下载设置新增“新书初始评论预加载”和“阅读时预加载下一章评论”。
- 存储清理新增评论缓存入口，并将原“下一章预读取缓存”明确改名为“下一章正文预读取缓存”；清理评论缓存不会删除任何已收藏评论。
- 保留 beta.9 运行时评论划线、按需获取、下一章评论预加载、整本评论获取及缓存管理；旧 notes EPUB 的读取兼容、旧评论迁移与 `Annotations:apply()` 暂时保留，待后续 beta 验证稳定后再瘦身删除。
