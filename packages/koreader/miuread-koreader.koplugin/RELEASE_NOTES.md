- 登录 修复设置文件保存格式异常；桌面/插件模式切换和扫码登录均增加保存确认，登录状态写入失败可直接重试，不再要求重新扫码。
- 下载 熄屏下载增加 Wi-Fi 链路守护；无线掉线时有限次数自动恢复，长时间无法联网会释放待机锁并安全休眠下载任务，保留章节断点等待恢复。
- 唤醒 用户主动唤醒时优先恢复界面与输入，下载任务不再长期占用待机锁阻挡唤醒；重新联网后可从原断点继续下载。
- 阅读 字号、字重和行距连续调整合并为最终一次排版；低内存或与下载重任务冲突时先让下载安全让路，降低白屏、卡死和异常退书。
- 本地书 修正格式判断与打开流程，未知格式不再由觅阅错误提前拦截；删除固定打开超时，本地书关闭继续绕过微信读书同步。
- 下载 修复网页字体被误判为正文图片、TAR 尾部异常导致整章作废和流式响应截断未被识别的问题；真正缺失的正文图片仍会阻止生成异常 EPUB。
- 同步 返回主页改为先完成本地保存、再后台完成原书最终云同步；弱网或切换下一本书时不会用旧会话阻塞主页或串错书籍。
- 稳定 保留 4.6.2 的正式 OTA、启动失败自动恢复、Bluetooth 超时保护和插件识别结构，不引入 Beta 更新通道。
