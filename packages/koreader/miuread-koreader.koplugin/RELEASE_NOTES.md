- 更新 新增正式/内测双通道选择，默认保持正式通道；选择会保存到本地，切回正式时若当前内测版本更高不会自动降级。
- 阅读 增加顶部中央轻点打开觅阅菜单，并拦截刚进入阅读页的残留点击；同步状态进一步区分同步中、待同步、待确认与失败。
- 主页 返回觅阅主页被休眠打断后会在唤醒时继续完成；主页电量复用 KOReader 实际电量，仅在数值变化时更新显示。
- 下载 目录请求遇到网络失败时进入明确等待并在恢复联网后继续，减少把断网误判为下载卡死；保留现有正式版熄屏下载与休眠逻辑。
- 发布 正式与内测发布流程统一到同一仓库，正式流程明确排除 Beta 标签，分别维护 `stable-channel` 与 `beta-channel`。
