- Kindle 修复伪锁屏偶发无法退出：翻盖打开与未被觅阅内部事件消费的电源键唤醒改为用户最高优先级，不再被旧的内部唤醒状态继续拦截。
- Kindle 内部 `powerButton` 事件改为一次性匹配，并在用户主动退出时立即恢复输入、隔离旧延迟回调；若解锁流程异常，1.25 秒后自动恢复可操作界面。
- 后台锁屏保护增加任务结束保险：下载完成、取消、休眠或异常退出后，即使最后一个界面回调丢失，也会再次安全释放；Kobo 仅复用该收尾保险。
- 兼容 保持现有 Kindle/Kobo 熄屏下载、下载完成后真实休眠、断点续传及阅读进度/时间/批注同步逻辑；Android 与未知设备继续不启用专用电源控制。
