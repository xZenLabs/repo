# 0.2.2

-- Make compatible with hardcover

# 0.2.1

-- Fix save and cancel buttons not showing up in certain menus

# v0.2.0

**Full Changelog**: https://github.com/burneracc0112/storygraph.koplugin/compare/v0.1.0...v0.2.0

-- added support for tracking progress based on page count (of the storygraph edition linked)
-- migrated settings to sidecar folder (plugin automatically handles migration)
-- improved the auto linking flow. audio books are filtered out. if an edition is being read / in want to read, it will automatically be linked. if there are multiple such editions, the one that was updated the latest will be linked. a different edition can be selected manually.
-- added disclaimer if local progress is behind storygraph progress and is being pushed manually (automatic syncing ignores such updates)

# v0.1.0
