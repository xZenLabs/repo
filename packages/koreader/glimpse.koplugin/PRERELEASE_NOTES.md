Fix #1 'Show in Book': it now jumps to the EXACT image, not the top of the chapter. The scanner records each image's DOM element path and Show-in-Book builds a precise crengine xpointer, validated against the image's filename before navigating (graceful fall-back to chapter-level if the book's normalized DOM disagrees). Verified on a real book: 44/45 images land on their own page, the one exception (cover) falling back cleanly.